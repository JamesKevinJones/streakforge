{
  "name": "streakforge",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "cd client && npx vite --port 5173",
    "build": "cd client && npx vite build",
    "preview": "cd client && npx vite preview",
    "postinstall": "cd client && npm install"
  }
}
